export interface PianoKeyProps {
  name: string;
  sound: string;
  keyboardPress: string;
  active: boolean;
}

export interface noteData {
  note: string;
  sound: string;
  keyboardPress: string;
}
